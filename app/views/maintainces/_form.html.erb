<%= form_with(model:  [@device,@maintaince], local: true) do |form| %>
  <% if @maintaince.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@maintaince.errors.count, "error") %> prohibited this maintaince from being saved:</h2>

      <ul>
      <% @maintaince.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
